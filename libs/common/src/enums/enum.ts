export enum ENVIRONMENT {
  LOCAL = 'local',
  DEVELOPMENT = 'dev',
  PRODUCTION = 'prod',
}

export enum LOG_LEVEL {
  DEBUG = 'debug',
  ERROR = 'error',
  LOG = 'log',
  VERBOSE = 'verbose',
  WARN = 'warn',
}
